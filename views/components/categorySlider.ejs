<!-- Categories -->
<div id="category-slider">
  <% categories.forEach(category => {
    let inactive = (activeCategory && activeCategory !== category.name) ? "inactive" : ""; %>
    <span class="badge <%= category.badgeName %> <%= inactive%>">
      <%= category.name %>
    </span>
  <% }); %>
</div>
